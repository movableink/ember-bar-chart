<DocsDemo as |demo|>
  <demo.example @name="getting-started-basic.hbs">
    <BarChart
      @barHeight={{10}}
      @barGap={{10}}
      class="w-full"
      as |chart|
    >
      <chart.Bar @value={{100}} />
      <chart.Bar @value={{50}} />
      <chart.Bar @value={{70}} />
    </BarChart>
  </demo.example>

  {{demo.snippet 'getting-started-basic.hbs'}}
</DocsDemo>
